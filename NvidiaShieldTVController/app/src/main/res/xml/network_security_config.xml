<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
         Allow cleartext (HTTP) for all connections.
         This app is designed for local Home Assistant servers which typically use HTTP.
         Users should ensure their Home Assistant instance is on a trusted network.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>